# CLAUDE.md

> Generated by the Evoke SDLC Kit Builder. Edit freely — this is your project's working agreement with Claude Code.

## Project technologies

- JavaScript
- Node.js

## Your toolbox

This kit installs agents, skills, and commands under `.claude/`.
Their descriptions are already loaded, so you know what each one is for.
Reach for them deliberately rather than improvising — they encode the
team's standards.

- Agents — dispatch via the Task tool (definitions in `.claude/agents/`)
- Skills — auto-activate by their description; their bodies live in `.claude/skills/`
- Commands — run as `/command` (definitions in `.claude/commands/`)

## How you work (operating procedure)

Follow this loop for **every request that changes code**. It is the whole
point of this kit — do not skip it and write everything in one pass. (A
trivial one-liner — a typo, a rename, a comment — can be done directly.)

**Durable state lives under `.agent/`** — these files, not the chat
transcript, are the source of truth:

- `.agent/plan.md` — the current goal's open tasks (`- [ ]`) and a short
  "current state / decisions" note. A backlog, not a log.
- `.agent/progress/NN-<task>.md` — one note per finished task: what changed,
  files touched, the validation verdict, anything the next task needs.
- `.agent/completed/YYYY-MM-DD-<goal-slug>.md` — a finished plan, archived
  whole once its last task is done.

Re-read `.agent/plan.md` at the start of every iteration.

1. **Plan before coding.** Restate the goal and break it into small, ordered,
   independently-verifiable tasks in `.agent/plan.md` (`- [ ]` each). That file
   holds the **current goal's open tasks only** — a backlog, not a log.
   For anything non-trivial, have `planner` produce the breakdown. If `.agent/plan.md` already exists, read it and resume from its
   first task; anything already finished has a note in `.agent/progress/` —
   trust those and never redo it.
2. **Do ONE task at a time, via the right specialist.** Take only the next
   unchecked task. Delegate it to the relevant specialist — dispatch via the Task tool (definitions in `.claude/agents/`). Route a build-a-feature task through the `spec-driven-builder` skill (PRD → stories → schema → API → tests).
3. **Stop at the verification checkpoint — do NOT commit yet.** Once the code
   for the task is written, hand it back to the human before going further:
   - State what changed and which files.
   - Give the **exact commands to run it and exercise the new behavior** —
     start the app, the `curl`/HTTP calls or test commands that hit the new
     endpoints, and the responses you expect.
   - Ask the human to run them and confirm the result looks right.
   Wait for that confirmation. Never commit or open a PR before the human has
   verified the change.
4. **On confirmation: validate, then commit.** Route the change through
   `validator` (and `security-reviewer` for security-sensitive changes), then record the outcome in `.agent/progress/NN-<task>.md`, **remove that task's
   line from** `.agent/plan.md` (the progress note is its record now — don't tick
   it and leave it behind), then commit with a clear message.
5. **Open a PR** for the committed work. A human owns the merge — you never
   self-approve or push to a protected branch.
6. **Continue, then archive.** Move to the next task. When `.agent/plan.md` has
   no tasks left, move the whole file to
   `.agent/completed/YYYY-MM-DD-<goal-slug>.md` — the goal, a one-line outcome
   summary, and the progress notes it produced — and start the next goal from a
   fresh, empty `.agent/plan.md`. Never let one plan file accumulate goal after
   goal. If you're unsure what comes next, ask the human — don't stop silently
   after a single change.

If you hand the whole goal to the `orchestrator` agent, it runs exactly this
loop for you. 
The two non-negotiables, on every tool: **one task at a time**, and **a human
runs and confirms the change before it is committed.**

## Audit trail

Every tool call in this kit is recorded to
`.agent/logs/agent-audit-YYYY-MM-DD.jsonl` (tool name, redacted inputs,
outcome), plus session-start and session-end records. Kit skills, hooks,
rules, and instructions are indexed in `.agent/logs/kit-inventory.json`.
Nothing leaves this machine.

- Active on: Claude Code sessions
- Hooks live in `.claude/hooks/` (`evoke_trace.py` + `evoke-trace.sh`)
- Requires Python 3 on PATH (or set `EVOKE_TRACE_PYTHON`)
- Scrubbing is best-effort; treat `.agent/logs/` as sensitive
- This is local developer telemetry, not tamper-evident evidence

- Never edit, move, truncate, or delete anything under `.agent/logs/`
  unless the user explicitly confirms. Run `/trace` to summarize.

## Organization guidelines

Organization-wide policies apply to every change. They are imported below,
so their full contents load into context automatically:

@.claude/guidelines/test-accessibility-baseline.md
@.claude/guidelines/ai-usage-accountability.md
@.claude/guidelines/security-baseline.md
@.claude/guidelines/separation-of-duties.md

## Engineering baseline

- Make the smallest change that fully solves the task. Don't refactor, add
  abstractions, or "improve" unrelated code unless asked.
- Match the conventions already in the file and the surrounding modules
  (naming, structure, error handling, test style).
- No dead code, no commented-out blocks, no half-finished implementations.
- Don't add comments that restate the code. Comment only non-obvious "why".
- Trust internal code and framework guarantees; validate only at system
  boundaries (user input, external APIs).
- When a requirement is ambiguous, ask before guessing. State assumptions you
  had to make.
## Iterative execution loop

For any task that takes more than one step, don't try to finish it in a single
pass and don't keep the plan only in your head. Drive it as a resumable loop
backed by files on disk, so the work survives a restart and you never lose the
thread between steps.

**Keep durable state under `.agent/`:**
- `.agent/plan.md` — the current goal, its ordered checklist of tasks that are
  still open (`- [ ]`), and a short "current state / decisions" section. This
  file, not the chat, is the source of truth for what is left to do. It is a
  backlog, not a log: a finished task is removed from it, so it shrinks as you
  work and never carries more than one goal.
- `.agent/progress/NN-<task>.md` — one note per finished task: what changed,
  files touched, the validation verdict, and anything the next task needs. This
  is where completed work is recorded.
- `.agent/completed/YYYY-MM-DD-<goal-slug>.md` — a finished plan, archived whole
  once its last task is done.

**The loop:**
1. **Plan once.** At the start, write `.agent/plan.md`: restate the goal and
   break it into small, ordered, independently-verifiable tasks (delegate the
   breakdown to `planner` for anything non-trivial). If `.agent/plan.md`
   already exists, read it and resume from its first task instead of starting
   over — everything listed is still open, and anything with a
   `.agent/progress/` note is done and must not be redone.
2. **Execute the next unchecked task only.** Pull the relevant plan lines and
   prior progress notes into the work so you don't re-discover the codebase.
3. **Stop at the human checkpoint — do NOT commit yet.** When the code is
   written, hand it back to the human: say what changed, give the **exact
   commands to run it and exercise the new behavior** (start the app, the
   `curl`/HTTP calls or test commands that hit the new endpoints, the responses
   you expect), and ask them to run it and confirm. Wait for confirmation before
   going further.
4. **On confirmation: verify, then commit.** Route the change through
   `validator` (and `security-reviewer` for security-sensitive changes), then
   commit with a clear message and open a PR for it — a human owns the merge, you
   never self-approve.
5. **Record + trim.** Write the progress note, then **remove that task's line
   from** `.agent/plan.md` and update "current state" — the progress note is its
   record, so don't tick it and leave it behind. On failure (or if the human
   reports a problem at the checkpoint), leave the task in the plan, capture the
   findings as a follow-up task, and loop on it.
6. **Repeat, then archive.** Move to the next task. When `.agent/plan.md` has no
   tasks left, move the whole file to
   `.agent/completed/YYYY-MM-DD-<goal-slug>.md` with a one-line outcome summary,
   and start the next goal from a fresh, empty plan file. If you're unsure what
   comes next, ask the human rather than stopping silently.

If the `orchestrator` agent is installed, hand multi-step goals to it — it runs
exactly this loop. Otherwise run the loop yourself. Either way: one task at a
time, intermediate outputs always written down, and never skip a governance hook
or failing test to "make progress" — surface the blocker in `.agent/plan.md`.
## Orchestration & delegation

You are the main agent. For any non-trivial task, orchestrate instead of doing
everything inline: decompose the goal, delegate focused pieces to specialist
agents, and verify their output before building on it.

- **Discovery first.** Before planning the first task on a checkout, confirm a
  project-context document exists (the `discovery` agent creates it) and read
  it, so you're not rediscovering the stack on every task.
- **Plan first.** Restate the goal, surface ambiguity, and break the work into
  ordered, independently-verifiable tasks before writing code.
- **Delegate to specialists.** Use the specialist agents available in your
  environment — e.g. `discovery` (project context), `planner` (read-only
  analysis), `code-reviewer`, `security-reviewer`, `test-author`,
  `playwright-test-author` / `e2e-runner` (QA), and `validator` (independent
  check). Give each a self-contained brief with the relevant context.
  - In Claude Code: use the Task tool to dispatch to `.claude/agents/`.
  - In Copilot: the user can invoke specialists via `@agent-name` in chat;
    for inline work, follow the specialist's guidelines directly.
  - In Cursor: apply the relevant rule for the specialist's domain.
- **One concern at a time.** Keep each task small enough that a single
  specialist plus one validation pass can finish it. Run independent tasks in
  parallel where the tool allows.
- **Verify before proceeding.** Route each specialist's output through the
  `validator` (and `security-reviewer` for security-sensitive changes) before
  relying on it. Separation of duties: the agent that writes code is not the one
  that approves it.
- **Never bypass governance.** Don't skip a hook, gate, or failing test to make
  progress — surface the blocker instead.
- **Human accountability.** A human owns every change merged to a protected
  branch; you assist, you do not self-approve.
## Responsible-AI guardrails

- A human is accountable for every change merged to a protected branch. AI
  assists; it does not approve its own work.
- Disclose AI-generated or AI-assisted changes per team policy (e.g. in the PR
  description).
- Never invent APIs, imports, config keys, or file paths. If you're not sure
  something exists, verify it (read the file / grep) before relying on it.
- Don't disable, weaken, or route around security controls, tests, or
  governance checks to "make progress". Surface the blocker instead.
- Keep regulated/sensitive data out of prompts and sample data unless the
  environment is approved for it. Prefer synthetic or masked data.
## Project type — Greenfield

This is a **new project**. There is little or no existing code to learn from,
so the requirements and user stories are the ground truth.

- Start from the **greenfield-starter** playbook skill: capture requirements,
  break them into user stories, and confirm the story list with the human
  before writing any code.
- Ask early about the intended repo layout (single repo vs. separate
  frontend/backend repos) and any existing inputs — UI designs, database
  schema, API contracts — and record them in the plan.
- Establish conventions first (project structure, lint/format, test setup,
  CI) so every later story lands on rails.
- Implement **one story at a time** through the operating procedure: plan the
  story, build it, stop at the human verification checkpoint, then commit.
## Project profile — Procurement

Known, human-confirmed facts about this specific project. Treat these as
ground truth and align every change with them — do not contradict or
rediscover them.

- **Available inputs:** User stories (Jira / Azure DevOps)
- **Architecture style:** Monolith
